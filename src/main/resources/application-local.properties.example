# Copy to application-local.properties (ignored by Git).
spring.datasource.url=${DB_URL:jdbc:postgresql://127.0.0.1:5432/ticketbooking}
spring.datasource.username=${DB_USERNAME:cinema}
spring.datasource.password=${DB_PASSWORD:cinema_local_password}
ticketbooking.jwt.base64-secret=${JWT_SECRET:}
cloudinary.url=${CLOUDINARY_URL:}
gemini.api-key=${GEMINI_API_KEY:}
gemini.model=${GEMINI_MODEL:}
